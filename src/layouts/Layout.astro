---
import Nav from '../components/Nav.astro';
import Hero from '../components/Hero.astro';
const year = new Date().getFullYear();
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="NGU - The Galactic Genius dApp. Discover our AI-powered meme creation and contesting platform where gravity meets genius and creativity knows no bounds." />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>$NGU - Galactic Genius dApp</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet" />
    <style>
      /* Global CSS Variables for Visual Branding */
      :root {
        --primary-color: #1e90ff;
        --secondary-color: #b21f1f;
        --accent-color: #fdbb2d;
        --background-color: #0a0a0f;
        --text-color: #e0e0e0;
        --header-footer-bg: #111;
        --border-color: #222;
        --faq-border: #333;
        --font-family: 'Montserrat', sans-serif;
        /* Blackhole & Gravity inspired gradient */
        --blackhole-gradient: radial-gradient(circle at center, #000, #111, #1a1a1a);
      }

      /* Global Styles */
      body {
        margin: 0;
        font-family: var(--font-family);
        background: var(--background-color);
        color: var(--text-color);
        overflow-x: hidden;
      }
      a {
        color: var(--primary-color);
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
      header, footer {
        background: var(--header-footer-bg);
        color: #fff;
        padding: 1rem 2rem;
        text-align: center;
        position: relative;
      }
      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
      }
      section {
        padding: 4rem 0;
        border-bottom: 1px solid var(--border-color);
      }
      h1, h2, h3 {
        font-weight: 700;
        color: #fff;
        margin-bottom: 1rem;
      }
      p {
        line-height: 1.6;
        margin-bottom: 1rem;
      }

      /* Header Top Bar for new buttons */
      .header-top {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        display: flex;
        justify-content: space-between;
        padding: 0.5rem 2rem;
        z-index: 10;
      }
      .buy-ngu {
        font-size: 1rem;
        color: var(--primary-color);
        text-decoration: none;
        background: var(--blackhole-gradient);
        padding: 0.5rem 1rem;
        border-radius: 4px;
        transition: transform 0.3s;
      }
      .buy-ngu:hover {
        transform: scale(1.05);
      }
      .dapp-dropdown {
        position: relative;
        display: inline-block;
      }
      .dapp-btn {
        font-size: 1rem;
        background: var(--blackhole-gradient);
        border: none;
        color: var(--primary-color);
        padding: 0.5rem 1rem;
        border-radius: 4px;
        cursor: pointer;
        transition: transform 0.3s;
      }
      .dapp-btn:hover {
        transform: scale(1.05);
      }
      .dapp-options {
        display: none;
        position: absolute;
        right: 0;
        top: 110%;
        background-color: var(--header-footer-bg);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        border-radius: 4px;
        overflow: hidden;
        animation: fadeIn 0.3s ease-in-out;
        z-index: 1000;
      }
      .dapp-options a {
        color: var(--text-color);
        padding: 0.5rem 1rem;
        text-decoration: none;
        display: block;
        transition: background 0.3s;
      }
      .dapp-options a:hover {
        background-color: var(--primary-color);
        color: #fff;
      }
      .dapp-dropdown:hover .dapp-options {
        display: block;
      }
      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(-5px); }
        to { opacity: 1; transform: translateY(0); }
      }

      /* Hero Animation inspired by gravitational pull */
      .hero {
        position: relative;
        text-align: center;
        padding: 6rem 2rem;
        background: linear-gradient(135deg, #1a2a6c, #b21f1f, #fdbb2d);
        background-size: 200%;
        animation: gradientAnimation 10s ease infinite;
        overflow: hidden;
      }
      @keyframes gradientAnimation {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
      }

      /* Responsive adjustments */
      @media (max-width: 768px) {
        .header-top {
          flex-direction: column;
          align-items: center;
          gap: 0.5rem;
        }
      }
    </style>
  </head>
  <body>
    <header>
      <!-- Header top bar with new navigation buttons -->
      <div class="header-top">
        <a class="buy-ngu" href="https://gmoon.fun/0xc1aa125280bd8dc4d747fa2170255369c3075976" target="_blank">Buy $NGU</a>
        <div class="dapp-dropdown">
          <button class="dapp-btn">Go to dApp</button>
          <div class="dapp-options">
            <a href="/auth/galactic-g">I'm a GG</a>
            <a href="/auth/guardian">I'm a Guardian</a>
            <a href="/api/discord/login-spectator">I'm a Spectator</a>
          </div>
        </div>
      </div>

      <h1>NGU</h1>
      <p>Where Gravity Meets Galactic Genius</p>
    </header>

    <!-- Navigation Bar -->
    <Nav />

    <!-- Hero Section -->
    <Hero />

    <div class="container">
      <!-- Content Sections -->
      <section id="about">
        <h2>About the dApp</h2>
        <p>
          NGU is not just a dApp—it’s a cosmic odyssey where cutting-edge AI propels meme culture into a new dimension. Dive into an experience that fuses advanced algorithms with boundless creativity, turning everyday memes into stellar works of art.
        </p>
      </section>

      <!-- Other sections... -->
      <!-- (Mission, Roadmap, FAQ, etc. remain unchanged) -->
    </div>

    <footer>
      <div class="container">
        <p>&copy; {year} NGU. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>
